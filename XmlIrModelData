<record id="{{$1:id}}" model="ir.model.data">
    <field name="name">{{$2:resource_external_id}}</field>
    <field name="model">{{$3:model.name}}</field>
    <field name="module">{{$4:module_name}}</field>
    <field name="date_init" eval="time.strftime('%Y-%m-%d %H:%M:%S')" />
    <field name="date_update" eval="time.strftime('%Y-%m-%d %H:%M:%S')" />
    <field name="res_id" eval="{{$7:1}}" />
    <field name="noupdate" eval="{{$7:True}}" />
</record>
